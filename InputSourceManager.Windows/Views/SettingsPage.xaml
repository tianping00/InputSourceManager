<UserControl x:Class="InputSourceManager.Windows.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="高级设置" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 通用设置 -->
                <GroupBox Header="通用设置" Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <CheckBox x:Name="ChkAutoStart" Content="开机自启动" Margin="0,0,0,8"
                                  Checked="ChkAutoStart_Checked" Unchecked="ChkAutoStart_Unchecked"/>
                        <CheckBox x:Name="ChkAutoSwitch" Content="启用自动切换" Margin="0,0,0,8"/>
                        <CheckBox x:Name="ChkShowIndicator" Content="显示输入法指示器" IsChecked="True" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="指示器显示时长 (毫秒):" VerticalAlignment="Center" Width="180"/>
                            <TextBox x:Name="TxtIndicatorDuration" Text="1200" Width="100" Margin="8,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!-- 快捷操作 -->
                <GroupBox Header="快捷操作" Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <Button x:Name="BtnResetConfig" Content="🔄 重置配置" 
                                Style="{DynamicResource MaterialDesignOutlinedButton}"
                                Click="BtnResetConfig_Click" Width="150" HorizontalAlignment="Left"/>
                    </StackPanel>
                </GroupBox>

                <!-- 关于 -->
                <GroupBox Header="关于" Margin="0,0,0,16">
                    <StackPanel Margin="16">
                        <TextBlock Text="Input Source Manager" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"/>
                        <TextBlock Text="版本: 1.0.7" Margin="0,0,0,8"/>
                        <TextBlock Text="受 InputSourcePro 启发的跨平台输入法管理工具" Margin="0,0,0,16"/>
                        <Separator Margin="0,0,0,16"/>
                        <TextBlock Text="功能特性:" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="• 自动上下文感知切换" Margin="20,0,0,4"/>
                        <TextBlock Text="• 应用程序、网站、进程规则" Margin="20,0,0,4"/>
                        <TextBlock Text="• 智能规则引擎" Margin="20,0,0,4"/>
                        <TextBlock Text="• 配置持久化" Margin="20,0,0,4"/>
                        <TextBlock Text="• 浏览器扩展集成" Margin="20,0,0,4"/>
                    </StackPanel>
                </GroupBox>

                <!-- 当前可用输入法 -->
                <GroupBox Header="当前可用输入法" Margin="0,0,0,16">
                    <DataGrid x:Name="DataGridInputSources" 
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              Margin="16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="输入法名称" Binding="{Binding}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>