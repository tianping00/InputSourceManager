using System.Windows.Controls;

namespace InputSourceManager.Windows.Views
{
    public partial class SettingsPage : UserControl
    {
        public SettingsPage()
        {
            InitializeComponent();
        }

        public void Initialize(object startupService, object manager, object configService)
        {
            // 简化版本 - 避免复杂的初始化
            // 实际功能将在后续版本中实现
        }
    }
}